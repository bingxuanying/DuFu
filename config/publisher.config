[publisher]
# the port at which subscribers will connect if no broker is created
port.pub = 5100

[service_discovery]
# Zookeeper connection string
connect = 127.0.0.1:2181

# timeout in ms for connecting to zookeeper
connection.timeout.ms = 6000