[port]
# the port at which subscribers will connect if no broker is created
pub = 5100

[service_discovery]
# Zookeeper connection string
connection.url = 10.0.0.1:2181

# timeout in s for connecting to zookeeper
connection.timeout.s = 6

[broker_server]
# Zookeeper connection port
port = 5001